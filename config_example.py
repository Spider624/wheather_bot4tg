# you should rename it to "config.py" when add real credentials
# for weather_api_token use api key from https://openw6eathermap.org/
# for marine_wheather_api_token use api key from https://www.weatherapi.com (I know that there a lot of info, but in 7 version of this bot we use only water temp from it)

telegram_token = "put_token_here"
weather_api_token = "put_token_here"
marine_wheather_api_token = "put_token_here"